pipeline {
  agent any
  tools {
    maven 'M2_HOME' 
  }
  stages {
    stage ('compile') {
      steps {
        bat 'mvn compile'	 
	  }
	 stage ('test') {
      steps {
        bat 'mvn test'	 
      }
	 }  
	}
  }
}
